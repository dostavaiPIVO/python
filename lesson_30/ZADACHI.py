from tkinter import *
from random import randint


# ЗАДАЧА 1

# root = Tk()
# root.geometry("500x500")
#
# label1 = Label(root, text="Логин:")
# label1.grid(column=0, row=0)
#
# label2 = Label(root, text="Пароль:")
# label2.grid(column=0, row=1)
#
# entry = Entry(root)
# entry.grid(column=1, row=0)
#
# entry = Entry(root, show="*")
# entry.grid(column=1, row=1)
#
# btn1 = Button(root, text="Авторизация")
# btn1.grid(column=1, row=2, sticky=E)

#root.mainloop()


# ЗАДАЧА 2

# root = Tk()
# root.geometry("500x500")
#
# def fu(pelmeni):
#     btn.place(x=randint(0, 400), y=randint(0, 450))
#
#
# btn = Button(root, text="ПОЙМАЙ МЕНЯ!")
# btn.place(x=15, y=27)
# btn.bind("<Enter>", fu)
#
#
#
#
# root.mainloop()